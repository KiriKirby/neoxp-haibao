{
  "name": "neoxp-haibao",
  "private": true,
  "version": "0.1.0",
  "description": "Desktop workflow and analysis engine migration for macrophage phagocytosis analysis.",
  "packageManager": "npm@11.12.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.8.2"
  },
  "scripts": {
    "sync:fiji-ref": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/sync-fiji-latest.ps1",
    "sync:fiji-ref:optional": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/sync-fiji-latest.ps1 -Optional",
    "refresh:fiji-baseline": "npm run sync:fiji-ref",
    "predev": "npm run sync:fiji-ref:optional",
    "predev:inspect-main": "npm run sync:fiji-ref:optional",
    "prebuild": "npm run sync:fiji-ref:optional",
    "prebuild:debug": "npm run sync:fiji-ref:optional",
    "pretypecheck": "npm run sync:fiji-ref:optional",
    "pretest": "npm run sync:fiji-ref:optional",
    "dev:ps": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/dev.ps1",
    "dev:ps:fast": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/dev.ps1 -SkipInstall",
    "dev:ps:inspect-main": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/dev.ps1 -SkipInstall -InspectMain",
    "build:packages": "npm run build --workspace=@neoxp/contracts && npm run build --workspace=@neoxp/parser && npm run build --workspace=@neoxp/workflow",
    "typecheck:packages": "npm run typecheck --workspace=@neoxp/contracts && npm run typecheck --workspace=@neoxp/parser && npm run typecheck --workspace=@neoxp/workflow",
    "dev": "npm run build:packages && npm run dev --workspace=@neoxp/desktop",
    "dev:inspect-main": "npm run build:packages && npm run dev:inspect-main --workspace=@neoxp/desktop",
    "build:debug": "npm run build:packages && npm run build:debug --workspace=@neoxp/desktop",
    "typecheck": "npm run typecheck:packages && npm run typecheck --workspace=@neoxp/desktop",
    "build": "npm run build:packages && npm run build --workspace=@neoxp/desktop",
    "test": "npm run test --workspaces --if-present"
  }
}
